This is an extension for Chrome app. It
should be easy enough to get started even if you've never built a Chrome
extension before by following the INSTALLATION notes.
It's all MIT-licensed.

INSTALLATION

  The dict works out of the box, so start by installing it in Chrome:

  - git clone git://github.com/wuzixiao/extension_dict.git my-project-name
  - Visit chrome://extensions and expand "Development mode" (top right).
  - Choose "Load unpacked extension ..." and point to the new project directory.
  - It's installed! You should see a new memu when you right-click on
  - the selected text. Click it!

It is a simple extension just for learning how to make Chrome extension.
